---
name: {{name}}
description: {{description}}
---

# {{name}}

## 概述

{{overview}}

## 适用场景

- 这个技能解决的核心问题：
  - <一句话写清楚这个技能替用户完成什么>
- 典型触发场景：
  - <用户通常在什么业务场景下会用到它>
- 不适合交给人工重复做的部分：
  - <哪些步骤重复、脆弱、容易出错，适合沉淀成技能>

## 触发示例

补 2 到 4 句真实用户会说的话，不要写成平台术语。例如：

- “帮我……”
- “把……做成工具”
- “我想自动化……”

## 推荐流程

先写一条最常见的默认流程，控制在 4 到 8 步：

1. 从什么前置条件开始：
   - <例如：已经登录后台、已经拿到链接、已经进入某页面>
2. 第一步做什么：
   - <进入哪个入口或读取什么输入>
3. 中间完成哪些关键动作：
   - <填写、选择、提交、查询、跳转>
4. 成功后得到什么结果：
   - <对象 id、详情页 URL、状态、文件等>

如果这个技能包含浏览器工具，建议额外说明：

1. 浏览器工具失败时，是否建议开启运行记录
2. 默认先读哪些文件：
   - `summary.json`
   - `timeline.jsonl`
3. 如果打开了多个页面，后续记录要结合 `pageId` 一起读

## 结果说明

至少说明：

- 关键工具会返回什么：
  - <id / URL / title / status / data>
- 哪些字段最值得给下游继续使用：
  - <例如：resultId、detailUrl、status>
- 哪些前提会影响结果：
  - <登录态、默认环境、浏览器状态、目录上下文等>

## 配置约定

如果这个技能需要配置，在这里明确：

- 需要哪些 key：
  - <例如：mySkill.baseUrl、mySkill.env>
- 每个 key 的含义：
  - <一句话说明用途>
- 推荐放在哪里：
  - <全局 `~/.cli-skill-config.json` 还是项目目录 `.cli-skill-config.json`>

平台不会自动给这个技能包一层独立命名空间，所以如果需要隔离，建议由技能自己定义 key 结构，例如：

- `{{name}}.baseUrl`
- `{{name}}.env`

## 使用边界

至少补这三类信息：

- 适合用的情况：
  - <什么时候直接用这个技能最合适>
- 不适合用的情况：
  - <什么时候应该换成人工判断、换 API、换别的技能>
- 需要先确认的前置条件：
  - <登录态、权限、环境、输入完整性等>

## 维护提示

这里不要只写“记得更新文档”，至少补清楚：

- 新增工具后，要同步更新：
  - 适用场景
  - 推荐流程
  - Tool Reference
- 输入、输出或默认值变更后，要同步更新：
  - 结果说明
  - 配置约定
  - 使用边界
- 改完 `src/skill/*` 后，要重新执行：
  - `cli-skill build`
- 如果技能包含浏览器工具，补清楚：
  - 失败后优先复盘本次运行记录
  - 不要直接盲改脚本

## Tool Reference

{{toolReference}}

## Config Reference

{{configReference}}
