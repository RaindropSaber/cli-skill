---
name: {{name}}
description: {{description}}
---

# {{name}}

## 概述

{{overview}}

## 适用场景

- 这个技能解决的核心问题是什么
- 用户通常会在什么场景下触发它
- 哪类任务适合交给这个技能，而不是人工操作

## 触发示例

建议补 2 到 4 句真实用户会说的话，例如：

- “帮我……”
- “把……做成工具”
- “我想自动化……”

## 推荐流程

建议说明：

1. 用户通常从什么页面、输入或前置条件开始
2. 这个技能会完成哪些关键步骤
3. 成功后通常会产出什么结果

如果这个技能包含浏览器工具，建议额外说明：

1. 失败时是否建议开启运行记录
2. 失败后先看哪几个文件
3. 是先复盘运行过程，再决定如何调整

## 结果说明

建议说明：

- 关键工具的输出是什么
- 哪些字段最值得继续被下游流程使用
- 有哪些默认值、登录态或环境前提

## 配置约定

如果这个技能需要配置，建议在这里明确：

- 需要哪些 key
- 这些 key 的含义
- 推荐放在全局还是项目目录的 `.cli-skill-config.json`

平台不会自动给这个技能包一层独立命名空间，所以如果需要隔离，建议由技能自己定义 key 结构，例如：

- `{{name}}.baseUrl`
- `{{name}}.env`

## 使用边界

建议说明：

- 哪些情况适合用这个技能
- 哪些情况不适合用这个技能
- 哪些前置条件不满足时，应该先停下来确认

## 维护提示

建议说明：

- 新增工具后，要同步更新本页说明
- 输入、输出或默认值变更后，要同步更新本页说明
- 改完文档模板后，要重新执行 `cli-skill build`
- 如果技能包含浏览器工具，建议补一句：
  - 失败后优先查看本次运行记录，而不是直接盲改脚本

## Tool Reference

{{toolReference}}

## Config Reference

{{configReference}}
